export class MyDate {
    date: number
    month: number
    year: number

    constructor(date: number, month: number, year: number) {
        this.date = date
        this.month = month
        this.year = year
    }
}
